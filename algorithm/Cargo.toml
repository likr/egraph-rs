[package]
name = "egraph-algorithm"
version = "0.1.0"
authors = ["Yosuke Onoue <onoue@likr-lab.com>"]
edition = "2018"

[dependencies]
egraph-graph = { path = "../graph" }
egraph-interface = { path = "../interface" }

